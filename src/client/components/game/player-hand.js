import "./player-hand.scss";

import React from "react";

import Card from "./card";

export default function PlayerHand({ hand, opSelectHand, selectCard, toggle, isOpen }) {
  return (
    <p>player hand</p>
  );
}